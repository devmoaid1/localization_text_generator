name: localization_text_generator
description: A sample command-line application.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.0.2

# Add regular dependencies here.
dependencies:
  glob: ^2.1.2
  auto_size_text: ^3.0.0
  flutter:
    sdk: flutter
  chalkdart: ^2.0.9
executables:
  glocalization: localization_text_generator
dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
